import React from "react";

const SlopeGraph = ({ slopes }) => {
  return (
    <div className="w-1/3">
      <div>This is where the slop Graph goes</div>
    </div>
  );
};

export default SlopeGraph;
